[build-system]
requires = [
    "setuptools>=64",
    "wheel",
    "Cython>=3.0"
]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
build = "cp38-* cp39-* cp310-* cp311-* cp312-* cp313-*"
skip = "*-musllinux_i686"
