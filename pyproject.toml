[build-system]
requires = [
    "setuptools>=64",
    "wheel",
    "Cython>=3.0"
]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
build = "cp313-*"
test-requires = ["pytest"]
test-command = "pytest {project}/tests -v"
